{
  "name": "learnfast-server",
  "version": "0.1.0",
  "private": true,
  "description": "LearnFast 동기화 서버. 로그인한 사람만 쓴다.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^5.2.1"
  }
}
