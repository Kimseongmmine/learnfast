# LearnFast — 프로젝트 기록

## 무엇
하루 계획을 짜주고, 배운 것을 다시 볼 때가 되면 **그 계획 안에** 넣어주는 웹앱.
따로 복습 화면이 없다. 다시 읽는 대신 안 보고 떠올리게 한다.

- 배포: GitHub Pages — https://kimseongmmine.github.io/learnfast/
  (`git push` → Actions 가 테스트를 돌리고, 통과하면 `client/` 를 올린다)
- **API 키 없이도 모든 기능이 돈다.** 서버·계정·결제 없음

## 왜
매일 "뭘 할지" 정하는 결정 자체가 실패 지점이라 그 결정을 앱에 넘긴다.
빽빽한 계획은 1일차에 포기하므로 **핵심 3개를 제시간에 시작하면 그날은 성공**이 무너짐 방지 장치다.

---

## 설명 방식 (항상 지킬 것)

**이 저장소의 작업 목적은 제작자의 학습이다.** 코드가 굴러가는 것보다 제작자가 이해하는 게 우선이다.
그래서 무엇을 하든 **초보자에게 설명하듯 자세히 적는다.** 짧게 끝내고 넘어가지 않는다.

말할 때 이 네 가지를 빼먹지 않는다:
1. **이게 뭐냐** — 새로 나온 단어·파일·명령어는 처음 보는 사람 기준으로 뜻부터 (예: "`npm` 은 남이 만든 코드를 받아오는 도구다")
2. **왜 이렇게 하냐** — 다른 방법도 있는데 왜 이걸 골랐는지. 선택지가 있었으면 그것도 같이
3. **지금 무슨 일이 일어났냐** — 명령어를 치면 화면에 뭐가 뜨는지, 그게 무슨 뜻인지
4. **안 되면 어떻게 보이냐** — 흔한 실패 모습과 그때 뭘 보면 되는지

새 파일을 만들면 **줄 단위로 읽어준다.** "여기서 이 줄이 하는 일은 …" 수준으로.
용어를 처음 쓰면 괄호로 풀어 쓴다 — 미들웨어, 포트, 환경변수, 라우트 전부 해당된다.
"보통 이렇게 합니다"로 끝내지 말고 **그 관습이 왜 생겼는지**까지 적는다.

이건 답변에만 적용되는 게 아니라 **커밋 메시지·코드 주석에도 적용된다.**

---

## 이 저장소의 범위

**학습 도구다.** 계획을 짜고, 안 보고 떠올리게 하고, 틀린 것만 다시 띄운다.

같은 제작자의 **개인용 계획앱 `LOOP` 은 완전히 다른 저장소다**(사주·식단·개인 기본값 포함, 비공개 대상).
한동안 브랜치로 같이 관리했지만 갈라놓았다 — 둘이 지향하는 게 다르고, 한쪽 기능이 다른 쪽에 안 맞는 일이 잦았다.
**그때 이름과 문서는 갈라놨는데 `localStorage` 키를 빠뜨려서 두 앱이 같은 데이터를 덮어썼다.**
`lf.` 접두사로 고친 경위는 아래 [데이터 모델](#데이터-모델-localstorage) 에 적어뒀다.
**여기에는 개인 정보가 한 글자도 없어야 한다.** 대학 이름·생년월일·특정인의 시간표가 들어가면 안 된다.

그래서 기본값을 채우지 않는다. 대신 **온보딩**(`renderOnboard`)이 과목·시험·장소를 받는다.
남의 대학 이름과 남의 시간표를 채워주는 건 도움이 아니라 오해다.

## 스택 (변경 금지)
- 바닐라 JS + HTML + CSS. **빌드도구·프레임워크·의존성 0**. 루트에 `package.json` 이 없다(서버는 `server/` 안에 따로 갖는다)
- 앱은 `client/` 안 파일 네 개: `index.html` / `app.js` / `style.css` / `manifest.json`
- 저장은 `localStorage` 뿐. 서버 없음. 데이터가 밖으로 나가지 않는다
- **PC 기준으로 만들었다.** 드래그 편집은 브라우저 기본 API(`draggable`)를 쓴다
- AI: Gemini 우선, 없으면 OpenRouter. 키는 브라우저에만. 없어도 전부 동작

### 렌더링
프레임워크 대신 `render()` 하나가 `root.innerHTML=""` 후 전부 다시 그린다.
`el(tag, {cls, text})` 헬퍼로 DOM을 만든다. 가상 DOM도 diff도 없다.
타이핑 중이던 입력은 `data-k` 키로 찾아 포커스·커서를 되돌린다(`restoreFocus`).

### 테스트
```
node tests/run.js      # 15개 스위트, 약 1.5초
```
`app.js` 끝의 두 줄이 브라우저와 node 를 가른다:
```js
if (typeof module !== "undefined" && module.exports) { module.exports = { ... }; }
if (typeof document !== "undefined") boot();
```
각 테스트는 **별개 프로세스**로 돈다 — 전역(`document`·`localStorage`·`fetch`·`Date`)을 가짜로 주입하므로
한 프로세스에서 여러 개를 돌리면 서로 오염된다. 네트워크도 실제 시각도 쓰지 않는다.

**`push` 하면 GitHub Actions 가 같은 명령을 돌리고, 실패하면 배포가 안 된다.**
깨진 앱이 사이트에 올라가는 길을 막는 관문이다(`.github/workflows/pages.yml`).

### 서버 테스트
```
cd server && npm test      # tests/run.js
```
**진짜 DB 도 네트워크도 안 쓴다.** `tests/fakedb.js` 가 `require.cache` 에 가짜를 미리 심어서
`src/db.js` 가 아예 안 읽히게 한다 — 앱 테스트가 `document`·`localStorage` 를 가짜로 주입하는 것과 같은 수법이다.
그래서 CI 가 `DATABASE_URL` 없이 돌린다.

`src/index.js` 는 `require.main === module` 일 때만 `listen` 한다.
안 그러면 테스트가 파일을 불러오는 순간 3000번 포트를 붙잡는다.
서버는 `app.listen(0)` 으로 **빈 포트에 띄우고 진짜 `fetch` 로 두드린다** — 상태 코드까지 실제와 같다.

`BCRYPT_COST` 를 주면 해시 비용을 낮춘다. **테스트에서만 쓴다**(4). 환경변수가 없으면 항상 12 라
배포에서 실수로 약해질 길이 없다.

**끝낼 때 `process.exit()` 를 쓰지 말 것.** 윈도우에서 닫히는 중인 소켓이 남은 채 강제 종료하면
`Assertion failed: !(handle->flags & UV_HANDLE_CLOSING)` 로 죽는다 —
검사는 전부 통과했는데 프로세스는 실패로 끝난다. `closeAllConnections()` 후 `process.exitCode` 만 정한다.

---

## 데이터 모델 (localStorage)

| 키 | 내용 |
|---|---|
| `lf.profile` | `{ fixed, rhythm, traits, prefs, places, courses, exams, goals[] }` |
| `lf.plans` | `{ "YYYY-MM-DD": { blocks[], generatedAt, source } }` |
| `lf.reviews` | 복습 큐 `[{ id, goalId, kind, text, q, missed, box, due, seen, made, leech }]` |
| `lf.sessions` | 세션 기록 `[{ date, blockId, kind, plannedMin, actualMin, breaks }]` |
| `lf.stuck` | `{ goalId: ["막혔던 지점", …] }` 최근 20개 |
| `lf.visits` `lf.energy` `lf.notes` `lf.done` `lf.events` | 접속·배터리·회고·완료·특별 일정 |
| `lf.onboarded` | 온보딩을 봤는지 |

- `goal` = `{ id, title, note, deadline, scope, hard, tasks[], analyzedAt }`
- `task` = `{ id, text, done, kind }`
- `block` = `{ id, time, text, place, kind, goalId, taskId, reviewId, first, core, done, started, startedAt, onTime, event, move }`

**접두사는 `app.js` 맨 위 `const KP = "lf.";` 한 줄에서 나온다.** 모든 키가 `KP + "이름"` 이라
다음에 또 갈라놓을 일이 생기면 열여덟 줄이 아니라 한 줄만 고치면 된다.

원래는 `loop.` 이었고 "바꾸면 데이터가 날아가니 그대로 둔다"고 적혀 있었는데, 그게 틀렸다.
두 앱이 `kimseongmmine.github.io` 아래 `/loop/` 와 `/learnfast/` 로 나란히 배포되는데
**`localStorage` 는 경로가 아니라 출처(origin) 단위**라, 경로가 달라도 같은 창고를 쓴다.
접두사가 같으면 이 앱을 여는 순간 개인용 계획앱의 진짜 데이터(프로필·계획·착수 기록)를 읽고 덮어썼다.
저장소를 갈라놓을 때 이름과 문서는 고쳤는데 정작 중요한 키를 빠뜨렸던 것이다.

**옛 키를 읽어오는 이사(migration) 코드는 일부러 안 넣었다.** 그걸 넣으면 남의 시간표를
"아무나 쓰라고 만든 앱"으로 그대로 옮겨오는 셈이고, 이 저장소가 피하려는 방향 그 자체다.
아직 사용자도 없다.

---

## 화면 (탭 · 해시 라우팅)
`오늘 📋` / `목표 🎯` / `기록 📈` / `설정 ⚙` — `#/goal` 처럼 해시로 이동.
`currentTab()` 은 `location.hash` 우선, 없으면 모듈 변수(테스트 환경엔 `location` 이 없다).
실행 모드와 온보딩은 탭바까지 포함해 화면을 통째로 차지한다.

**넓은 화면(900px 이상)에서는 왼쪽 세로 사이드바, 좁은 화면에서는 하단 고정 바다.**
`.tabbar` 는 원래부터 `position: fixed` 라 **HTML 은 한 글자도 안 바뀐다** — `#screen` 의 마지막 자식이어도
시각적 위치와 무관하다. 사이드바로 바뀌는 건 CSS 뿐이고 `flex-direction` 과 폭이 전부다.
PC 를 주 기기로 삼는 앱인데 탭 네 개가 화면 아래 끝에 붙어 있으면, 큰 모니터에서는 조작할 대상과
900픽셀쯤 떨어진 곳을 누르게 된다. 그래서 옮겼다.

돌아오는 길이 셋이다: 로고(홈 버튼) · `← 오늘` 버튼 · 탭바. 하나에만 의존하지 않는다.
**넓은 화면에서는 `← 오늘` 만 CSS 로 숨긴다**(사이드바가 늘 보이니 같은 일을 두 번 시키는 버튼이다).
DOM 에서는 지우지 않는다 — 좁은 화면에 필요하고 `nav.test.js` 가 존재를 단언한다.

---

## 학습 엔진

### 과제 유형 (`task.kind`) — AI에게 묻지 않는다
`개념 · 문제 · 유도 · 구현`. 과목 기본값(`courseKind`) 위에 문장 규칙(`TASK_KIND`)을 덮는다.
규칙이 무료 모델보다 정확하고 0원·0지연이다.

| kind | 최소 길이 | 첫 동작 | 인출 연습 |
|---|---|---|---|
| 구현 | **90분** — 컴파일·디버깅 사이클 | 터미널 열고 빌드/실행 한 번 | ✗ 코드는 백지가 안 통한다 |
| 문제 | 60분 | 첫 1문제만 | ✗ |
| 유도 | 60분 | 결과 식만 적어두고 백지 | ✓ |
| 개념 | 40분 | 목차만 보고 백지 | ✓ |

`retrievalText` 가 개념·유도 블록 앞에 `안 보고 써보기 —` / `안 보고 유도 —` 를 붙인다(두 번 안 붙는다).
`fillPlaces`: **구현 블록은 집**(노트북으로 되니 이동을 만들 이유가 없다).

### 복습 큐 (`lf.reviews`)
라이트너 상자 1~5, 간격 **`[1,2,4,8,16]`**. 전부 코드. AI를 안 부른다.
- **별도 화면이 없다.** `dueReviewCandidates` 가 오늘 차례가 된 것을 `nextPendingTasks` 앞에 놓아 그냥 계획의 블록이 된다
- 복습 블록은 `REVIEW_MIN`(30분). `isMicroBlock` 의 25분 기준에 걸리면 얇은 줄로 접혀 못 누른다
- **간격이 시험 날짜를 안다** — 다음 차례가 시험을 넘기면 `EXAM_MARGIN`(2)일 전으로 당긴다
- `isLeech`: `seen ≥ 6` 인데 상자가 1이면 열흘 물리고 "쪼개라"고 표시한다
- `reviewQuota`: 가장 가까운 시험까지 8일 이상 2개 / 7일 이내 4개 / 3일 이내 5개

### 큐는 스스로 찬다 · 인출 확인
개념·유도는 **한 줄을 안 적어도** 완료할 때 큐에 들어간다(`isRecallable`).
한 줄에만 의존하면 타이핑을 한 번도 안 한 사람에게는 간격 반복 전체가 죽은 기능이 된다.

복습 블록에는 완료 대신 **`✓ 기억났다` / `✗ 안 나왔다`** 두 개(`.recallrow`).
`settleReview(id, note, today, correct)` 의 네 번째 인자가 판정이다. 안 주면 한 줄이 비었는지로 본다.

### 한 줄만 묻는다
블록을 끝낼 때 kind 별로 한 줄(`askLabel`): 구현 `막혔던 지점?` · 문제 `틀린 번호?` · 유도 `막힌 단계?` · 개념 `안 나온 것?`
**비우고 넘어가는 게 기본이다.** 수동 입력 부담은 앱 이탈의 문서화된 원인이라 한 줄을 넘지 않는다.
`finishBlock` 이 유일한 완료 경로다. 초안은 `askDraft[blockId]` 에 둔다(5분 타이머가 1초마다 다시 그린다).

### 세션 기록
기록이 목적이 아니라 **"90분 계획을 실제 55분 한다"를 알아내 블록 길이를 현실로 끌어내리는 것**이 목적이다.
`sessionStats` 는 유형당 `SESSION_MIN_N`(3)건이 쌓이기 전에는 `null` — 표본 두 개로 말하지 않는다.
중단 횟수는 `visibilitychange` 로 센다. **사실만 세고 화면에 평가를 쓰지 않는다.**

### 하루 세 과목 · 시험 역산
`dailyCourses` 가 계획 후보를 뽑기 전에 과목부터 셋으로 줄인다(마감 임박 ×2 + `goal.hard` +15 + 며칠 안 했는지 ×3).
**결정론적이다** — 같은 날 같은 상태면 같은 답. 무작위를 쓰지 않는다.
`paceLine`: `D-54 (중간고사) · 6장 중 1.5장 · 남은 4.5장 → 12일에 1장`. 하루 1단위 미만이면 "N일에 1장".
마감을 안 적으면 `effectiveDeadline` 이 학기 시험(`profile.exams`)에서 물려받고 **출처를 밝힌다**.

### 브리지 — 앱이 기억하고, 챗앱이 생각한다
무료 API 티어를 쥐어짜지 않는다. `buildPrompt(kind, ctx)` 가 앱이 아는 사실을 프롬프트에 박아 클립보드로 복사한다.
`breakdown`(과제 쪼개기) · `retrieval`(인출 문제) · `stuck`(막힘) · `pace`(진도 점검).
**`PROMPTS` 상수가 이 기능의 본체다.** 개선하려면 여기만 갈아끼우면 된다.
왕복이 닫힌다 — 답을 붙여넣으면 `parseCourseTasks` / `parseQuestions` 가 읽어 과제·문제가 된다.

### 과목 팩
목표 하나를 통째로 남에게 준다(`exportPack` / `importPack`).
**개인 기록은 절대 안 담는다** — 완료·상자·다음 차례·본 횟수·틀린 지점·세션 전부 제외.
받는 사람은 1번 상자부터 시작한다. 테스트가 **있어야 할 필드보다 없어야 할 필드**를 검사한다.

---

## 무료 티어 하네스 (반드시 지킬 것)
- **하루 API 콜은 시간표 배치 한 번뿐이다.** 과제 쪼개기는 브리지로 한다
  (예전엔 목표마다 1콜이라 5과목이면 6콜이 몇 초 안에 나가 한도를 바로 넘겼다)
- `geminiChat` 은 429/503 에 1.2초 쉬고 한 번 재시도한 뒤 `GEM_FALLBACK_MODELS` 를 순회한다
- `aiChat` 은 **교차 폴백**한다: Gemini 가 죽고 OpenRouter 키가 있으면 그쪽으로
- **계획 JSON 은 4필드**(`time·text·ref·core`). `place`·`first`·`kind` 는 코드가 채운다
- 한도 초과(`isQuotaError`)는 다른 실패와 구분해서 브리지로 안내한다

---

## 원칙
1. **결정을 사용자에게 되돌려주지 않는다.** 빈 입력창을 만들지 않는다
2. **앱은 사용자를 평가하지 않는다.** 칭찬도 위로도 잔소리도 없다. 사실만 적는다
3. **하지 않은 것을 한 것처럼 세지 않는다**
4. **AI 실패는 상시 조건이다.** 키가 없거나 한도를 넘겨도 모든 기능이 돈다
5. **없는 숫자는 지어내지 않는다.** 표본이 모자라면 그 줄을 아예 안 그린다

1번은 "기본값을 채운다"가 아니라 **"온보딩이 대신 받는다"**로 구현된다.

`place` · `day` · `onboard` 테스트는 `m.MY_PLACES` 존재 여부를 보는데, 이 저장소에는 없으므로
언제나 "기본값을 안 채운다" 쪽을 검사한다. 그 분기는 개인용 갈래가 있던 시절의 흔적이고 그대로 둬도 무해하다.

---

## 측정으로 정한 것
`[1,2,4,8,16]` 간격은 15주 학기(54일 뒤 시험)를 시뮬레이션해서 골랐다.
벼락치기와 손대는 횟수를 같게 맞췄을 때 — 시험 당일 94.0 / 2주 뒤 88.7 (옛 `[1,3,7,16,35]` 는 91.2 / 75.2).

**정직하게 적자면: 시험 하나만 보면 벼락치기와 거의 같다(1.01배).** 차이는 2주 뒤에 난다(12.5 vs 88.7).
비용으로 재면 단일 시험은 벼락치기가 5.9배 싸다. 이 설계의 값어치는 기말과 후속 과목에 남는 양이다.

시뮬레이션이 **실제 결함도 찾았다** — 27일차에 4번 상자에 오른 항목의 다음 차례가 62일차라
중간고사(54일)까지 5주 동안 안 보였다. 그래서 간격이 시험 날짜를 보게 됐다.

---

## 안 넣기로 한 것 (근거 있음)
- **배운 날 저녁 굳히기** — 그럴듯해서 재봤는데 회상이 91.3% → 85.4% 로 떨어졌다.
  실패율은 줄지만 손댄 횟수만 늘고 이득이 없다. 아직 잘 기억하는 걸 다시 보면 간격 효과가 거의 안 붙는다
  (`gain = base^(1-p)`, p 가 높으면 1에 수렴). 4/7/12시간 뒤 전부 나빴다. **다시 넣지 말 것**
- 벌칙형 커밋먼트(55% 실패) · 알림 강화(앱 이탈 1위 원인) · 버디·소셜(자동 코치와 차이 없음이 RCT로 확인)
- 21일 챌린지(자동화까지 18~254일, 정해진 기간 없음)
- **분석 도구(GA 등)** — 붙이는 순간 "데이터가 밖으로 안 나간다"가 거짓이 된다. GitHub Insights → Traffic 으로 충분하다

---

## 함정 (다시 밟지 말 것)

**테스트에 `...Z` 로 끝나는 절대 시각을 박지 말 것.** 시간대에 따라 결과가 달라진다.
`startedAt:"2026-08-27T00:02:00.000Z"` 는 한국에서 09:02 지만 UTC 에서는 00:02 다.
그런데 "지금"은 `new RealDate(2026,7,27,10,2,0)` 처럼 **로컬 시각**으로 만들어서,
한국에서는 경과 60분이 나오고 GitHub Actions(UTC)에서는 600분이 나왔다.
**양쪽 다 로컬로 맞춘다** — `startedAt: new RealDate(2026,7,27,9,2,0).toISOString()`.
확인은 `TZ=UTC node tests/run.js` 로 한다(윈도우 git bash 에서도 먹는다).

**줄바꿈이 섞여 있다.** `client/app.js` 는 LF, `client/style.css` 는 CRLF 인 구간이 있다.
패치 스크립트는 파일에서 우세한 쪽을 감지해서 쓸 것:
```js
const NL = ((s.match(/\r\n/g)||[]).length*2 > (s.match(/\n/g)||[]).length) ? '\r\n' : '\n';
```

**같은 줄이 두 군데 있다.** `profile.goals.forEach(function (g) {` 는 진행도와 설정 양쪽에 있다.
범위를 잘라낼 때 함수 시작 인덱스부터 찾지 않으면 엉뚱한 곳을 자른다.

**블록을 잘라내면 사이에 낀 것이 같이 날아간다.** 실제로 식단 블록을 자르면서
`parseExams` · `nextExam` · `effectiveDeadline` · `generating` 이 사라졌다.
문법은 통과하고 실행하면 죽는 종류다. **자른 뒤 반드시 `node tests/run.js`.**

**`const` 배열이 뒤에 선언된 상수를 참조하면 TDZ 로 앱이 통째로 안 뜬다.**
`dataKeys()` 가 상수가 아니라 함수인 이유다.

**CSS 미디어 쿼리는 덮으려는 기본 규칙 뒤에 둘 것.** 앞에 두면 일부 속성만 적용돼
데스크톱에서 탭바가 화면 밖으로 나간다. 실제로 한 번 그랬다.
그래서 `@media (min-width: 900px)` 블록은 **`style.css` 맨 끝 한 곳에만** 있다.
`nav.test.js` 가 스타일시트를 글자로 직접 읽어 순서를 검사한다.

사이드바 레이아웃에서 다시 밟기 쉬운 것 셋 — 전부 `nav.test.js` 가 단언한다:
- **폭과 본문 여백은 `--sidew` 하나로 묶는다.** 숫자를 두 군데 적으면 언젠가 어긋나
  본문이 사이드바에 깔리거나 반대로 빈 띠가 남는다
- **위치를 `transform` 으로 만들지 말 것.** 옛 버그가 `translateX` 로 자리를 잡다가 났다.
  `left`/`right` 로만 붙인다
- **`.tab` 의 `flex: 1 1 0` 을 반드시 풀 것.** 가로 바에서는 넷이 폭을 나누라는 뜻이지만,
  세로로 세우면 그대로 **화면 높이를 4등분**해 탭 하나가 손바닥만 해진다

실행 모드·온보딩은 `render()` 가 탭바를 아예 안 그린다. 그때 남는 240px 빈 여백은
`body:has(#screen > .focus)` 로 지운다 — DOM 은 그대로 두고 CSS 가 알아서 눈치채게 한다.

**행 안의 버튼은 `e.preventDefault()`.** 블록 행이 `<label>` 이라 클릭이 체크박스로 샌다.

**배포 직후 응답 헤더에 `x-render-routing: no-server` 가 보이면 기다려라.**
첫 배포가 끝나기 전에 주소를 찔러보면 Render 의 입구(엣지) 여러 대 중 일부가 "뒤에 프로그램 없음"으로
기억해버린다. 그 뒤로 같은 주소가 **200 과 404 를 오간다** — 어느 입구로 들어가느냐에 따라 갈린다.
서버가 죽은 게 아니라는 증거는 `/health` 의 `uptime` 이다. **리셋 없이 계속 늘면 인스턴스는 멀쩡하다.**
실제로 0단계에서 0/5 → 10/20 → 13/14 로 10분쯤 걸려 저절로 수렴했다. 재배포도 필요 없었다.
급하면 `Manual Deploy → Deploy latest commit` 이 모든 입구에 등록을 다시 방송한다.

**bash 로 한글·백틱·이모지를 다루지 말 것.** 백틱은 명령 치환으로, 이모지는 명령으로 해석된다.
문서·코드를 고칠 때는 스크래치패드에 스크립트를 쓰고 `node` 로 실행할 것.

---

## 백엔드 — 0단계 완료, 다음은 1단계

**살아있는 서버:** https://learnfast-server.onrender.com/health → `{"ok":true,"service":"learnfast","uptime":N}`
Render 가 `main` 브랜치를 보고 있어서 **`git push` 하면 자동으로 다시 배포된다**(앱의 Pages 와 같은 방식).
로컬에서 켜기: `cd server` 후 `npm start`(그냥 켜기) 또는 `npm run dev`(저장할 때마다 자동 재시작).

**목표는 제품 필요가 아니라 학습이다.** 제작자가 풀스택을 한 번 끝까지 해보려는 것이고,
그래서 BaaS 를 쓰지 않고 **Node + Express + Postgres 를 직접 짠다**. REST 설계·스키마·JWT·배포를 손으로 한다.

### 지켜야 할 경계
**클라이언트는 의존성 0 을 유지한다.** 서버만 의존성을 갖는다. 이 경계를 흐리지 말 것.

```
learnfast/
├── client/                    ← 프론트. 의존성 0, 빌드도구 없음
│   └── index.html  app.js  style.css  manifest.json
├── tests/                     ← 프론트 테스트 15개
├── server/                    ← 백. Express + Postgres
│   ├── src/         index.js  db.js  auth.js
│   ├── migrations/  DB 스키마를 만든 SQL. 번호 순서가 곧 의미다
│   ├── tests/       서버 테스트
│   └── .env         커밋 금지 · .env.example 만 커밋
└── .github/workflows/pages.yml   ← client/ 를 Pages 로 올린다
```
**DB 는 폴더가 아니다.** Supabase 안에서 도는 서비스라 저장소에 실체가 없다.
저장소에 있어야 하는 건 **그 DB 를 어떻게 만들었는지 적은 SQL**(`server/migrations/`) 뿐이다.

Pages 는 원래 루트나 `docs/` 만 서빙한다. `client/` 는 못 고르므로 **워크플로가 대신 올린다** —
테스트가 통과해야 배포되고, 올라가는 건 `client/` 뿐이라 `server/` 는 사이트에 안 실린다.
**그래도 저장소가 공개라 커밋된 것은 누구나 읽는다. 비밀값은 절대 커밋하지 않는다.**

### 로그인은 옵션이다 (핵심 제약)
```
미로그인 → 지금과 100% 동일. localStorage 만. 서버로 아무것도 안 보낸다
로그인   → 기기 간 동기화가 추가된다
```
README 의 약속은 이미 **"로그인하지 않으면 서버로 아무것도 보내지 않습니다"** 로 고쳐 뒀다(0단계에서 같이).
"서버가 없고"는 이제 사실이 아니므로 "앱은 서버를 쓰지 않고"로 바꿨다. 1단계에서 로그인이 붙어도 이 문장은 그대로 참이다.
로그인을 필수로 만들면 이 앱의 강점("열면 바로 쓴다", "데이터가 안 나간다", "운영비 0원")이 전부 사라진다.

### 단계 — 각 단계 끝에 동작하는 것이 남아야 한다
| | 무엇 | 대략 |
|---|---|---|
| 0 | ~~서버 뼈대 + **배포까지**. `GET /health` 하나만~~ **완료 (2026-08-27)** | 반나절 |
| 1 | 회원가입·로그인. bcrypt + JWT | 하루 |
| 2 | 통짜 백업 동기화. `PUT /backup` · `GET /backup` | 하루 |
| 3 | **레코드 단위 동기화** — 오프라인 우선, 충돌 해결 | 2~3일 |
| 4 | 토큰 갱신 · rate limit · 에러 처리 | 하루 |

**0단계를 배포까지 먼저 뚫는다.** 서버는 로컬에서 잘 돌다가 배포에서 막히는 일이 제일 흔하다.
빈 서버로 그 길을 먼저 내놓으면 나머지가 편하다.

3단계가 진짜 배우는 곳이고, 답해야 할 질문은 이렇다:
- 같은 복습 항목을 PC 에서 "기억났다", 폰에서 "안 나왔다" 눌렀으면?
- 비행기 모드로 사흘 쓰다가 켜면?
- 서버에서 지운 것을 다른 기기가 아직 갖고 있으면? (tombstone)

설계 방향: 레코드마다 `updatedAt`, `POST /sync` 로 `{ since, changes[] }` 를 주고받고,
레코드 단위 last-write-wins + 삭제는 tombstone. 시계는 서버 것을 기준으로 한다.

### 배포처 (계정은 제작자가 만들어야 함)
**Supabase(DB) + Render(서버)** 를 권한다.
- Render 의 **무료 Postgres 는 90일 뒤 삭제**된다. 학습 프로젝트에 치명적이라 DB 는 Supabase 를 쓴다
- Supabase 는 Postgres 만 빌리고 BaaS 기능은 안 쓴다 — "직접 짠다"는 목적이 유지된다
- Render 무료 웹서비스는 15분 놀면 잠든다(첫 요청이 느림). 학습용엔 무방

**Render 에 실제로 넣은 설정** (다시 만들 일이 생기면 그대로 쓸 것):

| 칸 | 값 |
|---|---|
| Root Directory | **`server`** — 비우면 루트에서 `package.json` 을 찾다가 빌드가 죽는다 |
| Build Command | `npm install` (켜기 전 한 번) |
| Start Command | `npm start` (그다음 계속) |
| Region | Singapore |
| Instance Type | Free |

환경변수는 하나도 안 넣었다. `PORT` 는 Render 가 알아서 준다(10000번).

### 안 할 것
- 결제·구독 — 사용자가 생기기 전엔 추측이다
- 클라이언트에 프레임워크·빌드도구 도입 — 서버가 생겼다고 앱까지 바꾸지 않는다
- 분석 도구 — 서버가 생겨도 이건 그대로 안 붙인다

---

## 손으로 확인할 것
`node tests/run.js` 가 15개를 덮지만, 화면은 눈으로 봐야 한다.
- **온보딩** — 시크릿 창으로 열어야 `localStorage` 가 비어서 첫 화면이 보인다
- **키 없이** 계획 생성 · 복습 · 브리지 복사까지 되는지
- **팩 왕복** — 내보낸 JSON 을 열어 개인 데이터가 없는지 눈으로
- **착수한 두 시간짜리 블록이 끝나고 완료되는지** — 이걸 놓치면 앱이 망가진다
